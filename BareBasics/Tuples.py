#tuples are immutable essentially an array from java
list = [1, 2, 3]
tuple = (1 , 2, 3)
print (tuple [0])
print (tuple.count (1))

#cannot change values in a tuple, more strict array list
#tuple [0] = 10